import * as uuid from 'uuid';

export const NAME = 'pattern';

export const MIN_THREADS = 2;
export const MIN_TABLETS = 4;
export const MIN_ROWS = 1;

export const initialThreadIds = [uuid.v4(), uuid.v4()];
